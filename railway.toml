[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build --workspace=@ekskomen/backend"

[deploy]
startCommand = "npm run start --workspace=@ekskomen/backend"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
