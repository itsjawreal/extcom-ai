{
  "name": "@extcom-ai/extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --mode sw && vite build --mode popup",
    "dev": "vite build --watch",
    "icons": "node scripts/generate-icons.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/chrome": "^0.0.327",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
